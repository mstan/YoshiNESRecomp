build/
*.nes
*.bin
*.sav
*.log
*.zip
.mcp.json

# nesrecomp is cloned by setup.sh / setup.bat into this directory.
# Pinned SHA recorded in nesrecomp.pin and enforced at CMake configure time.
nesrecomp
nestopia-core

# Pre-migration backup of the previously vendored nesrecomp tree (kept
# locally for reference, including its embedded .git with preserved/* tags).
nesrecomp.vendored.bak/

# Generated-output rollback backups
generated/*.preregen.bak
